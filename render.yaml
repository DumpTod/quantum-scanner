services:
  - type: web
    name: quantum-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 600 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
